package com.gabrielbarrilli.LocalDataTime;

import java.time.LocalDate;
import java.time.LocalTime;
import java.time.format.DateTimeFormatter;

public class FormatoDeTempo {
/*
    public void FormatarTempo() {

        System.out.println();

        LocalDate dataAtual = LocalDate.now();
        System.out.println("Data não formatada: " + dataAtual);

        DateTimeFormatter formatoData = DateTimeFormatter.ofPattern("dd/MM/yyyy");
        String dataFormatada = dataAtual.format(formatoData);

        System.out.println("Data formatada: " + dataFormatada);

        LocalTime horaAtual = LocalTime.now();
        System.out.println("Hora não formatada: " + horaAtual);

        DateTimeFormatter formatoHora = DateTimeFormatter.ofPattern("HH:mm:ss");
        String horaFormatada = horaAtual.format(formatoHora);

        System.out.println("Hora formatada: " + horaFormatada);

        System.out.println("");

    } */
}
